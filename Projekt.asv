close all;

%zmienne globalne
global hNXT;
global speed;

%tworzenie instanci itp
Motors = InitMotors();

%testy sterowanie itd
Thetas45=[45,45,45];
ThetasZero=[0,0,0]; %sprawdzenie czy skalibrowany
SetAngles(Motors, thetas);


%co do dorobienia
Robot1=CreateRobot(Motors,a1,a2,a3)%parametry robota

positionZero=[0,0,0];
positionVert=[0,0,0]'
position1

MoveEndpointToPositionXYZ(Robot,position)

 
 
 
 
 
 
 
 
 %zamykanie polaczen
 cc(hNXT);